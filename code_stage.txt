
  const makeList = data =>{
    
    console.log(Array.isArray(props.tit[0]))
    
    if(Array.isArray(data)){
      return <h2>
            <small>{data[0]}</small><br/>
            {data[1]}
        </h2>
    }else{
      return <h2>{data}</h2>
    }
  }




  <div className="cbx bgi bg1-3">
      {makeList(props.tit[2])}
  </div>

  
        history.pushState(null, null, '?cat='+encodeURIComponent(val));